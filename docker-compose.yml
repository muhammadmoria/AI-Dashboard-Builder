version: "3.9"
services:
  nexus:
    build: .
    ports: ["8501:8501"]
    volumes: ["./:/app"]
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - JWT_SECRET=${JWT_SECRET:-change-me}
    restart: unless-stopped